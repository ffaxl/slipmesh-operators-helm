apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: mesh
  namespace: {{ include "slipmesh.namespace" . }}
  labels:
    {{- include "slipmesh.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: mesh
  template:
    metadata:
      labels:
        app: mesh
        {{- include "slipmesh.labels" . | nindent 8 }}
    spec:
      serviceAccountName: mesh
      hostNetwork: true
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      tolerations:
        {{- toYaml .Values.tolerations | nindent 8 }}
      containers:
        - name: mesh
          image: {{ .Values.image.mesh }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            capabilities:
              drop: ["ALL"]
              add: ["NET_ADMIN"]
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: RUST_LOG
              value: {{ .Values.logLevel.mesh | quote }}
          resources:
            {{- toYaml .Values.resources.mesh | nindent 12 }}
